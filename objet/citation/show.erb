<%
# Pour l'affichage d'une citation d'auteur
page.description= "Affichez la citation d'un auteur et son explicitation pour mieux la comprendre."
enable_comments
%>
<%
if user.subscribed? || user.admin?
  is_consultable = true
else
  app.session['citations'] ||= []
  app.session['citations'] << site.current_route.objet_id
  app.session['citations'] = app.session['citations'].uniq
  is_consultable = app.session['citations'].count < 5
end
%>
<%= Citation.titre_h1 %>
<%= Citation.current.output %>
