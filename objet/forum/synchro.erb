<%
# Vue pour la synchronisation
raise_unless_admin
%>
<%= forum.titre_h1("Synchronisation") %>
<%
site.require_module( 'remote_file' )

def rfile
  @rfile ||= ::RFile::new("./database/data/forum.db")
end
%>
<%=

  forum.rfile.bloc_synchro(verbose:true, buttons: :both)

  %>
