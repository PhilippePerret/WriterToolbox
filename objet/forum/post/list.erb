<%
# Vue affichant les messages du forum
#
# Par défaut, la page affiche les 30 derniers messages. Sinon, affiche
# les messages du sujet voulu par la route.
%>
<%= forum.titre_h1("Messages", {onglets: true}) %>

<%= forum.posts.navigation_bar(:top) %>
<ul id="posts"><%= forum.posts.list(as: :li, from: :last) %></ul>
<%= forum.posts.navigation_bar(:bottom) %>
