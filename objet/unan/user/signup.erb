<%
# Formulaire d'inscription au programme Un AN UN SCRIPT
#
# Noter que deux solutions sont possibles : soit le visiteur est
# déjà inscrit et il a juste à payer son inscription, soit le visiteur
# est totalement inconnu et il faut alors lui proposer le formulaire
# d'inscription du site en général, plus l'inscription au programme.
%>
<%= Unan::titre_h1 %>

<% if user.unanunscript? %>
  <p>Vous êtes déjà inscrit<%= f_e %> à ce programme, vous ne devriez pas avoir à repasser par ici ;-)…</p>
<% else %>
  <h2>Inscription</h2>
  <% if user.subscribed? %>
    <p>Vous êtes déjà abonné au site, il vous suffit donc de vous inscrire au programme “Un An Un Script” pour le suivre.</p>
    <%= user.check_abonnement %>
  <% elsif user.identified? %>
    <p>Vous êtes enregistré<%= f_e %> sur le site, mais vous n'avez pas d'abonnement courant. En conséquence, notez que l'inscription au programme “Un An Un Script” offre <strong>deux ans d'abonnement</strong> au site pour une utilisation complète de <a href="tool/list" target="_new">tous les outils</a>.</p>
  <% else %>
    <p class='small'>L'inscription au programme “Un An Un Script” vous abonne aussi pendant deux ans au site et vous offre donc un complet accès à tous les autres outils. Merci de bien vouloir remplir le formulaire ci-dessous.</p>
    <%=
      app.require_optional '_per_route/user/signup'
      page.view('user/signup')
    %>
  <% end %>
<% end %>
