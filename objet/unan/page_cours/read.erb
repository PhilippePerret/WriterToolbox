<%
# Vue pour affichage d'une page de cours
#
# Noter que puisqu'on rejoint cette vue par un context "unan" alors que
# la class est dans "Unan::Program::PageCours" (donc il faudrait un context
# défini à "unan/program") la route ne peut pas définir l'objet comme
# instance, il faut donc le prendre ici.
page_cours = page_cours(site.current_route.objet_id)
%>
<%= Unan::titre_h1 %>
<% if user.admin? || OFFLINE %>
  <%= page_cours.link(titre:"[Adminstrateur] Éditer la page ##{page_cours.id}", edit: true).in_div(class:'small fright right') %>
<% end %>
<%= page_cours.output %>
