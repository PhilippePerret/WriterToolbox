<%
# Page d'accueil de l'utilisateur dans Un An Un Script
%>
<% unless user.unanunscript? %>
  <%= Unan::titre_h1 %>

  <%
    # LES OUTILS SI L'UTILISATEUR EST L'ADMINISTRATEUR
    if user.admin? %>
      <div class='admin_tools right small btns'>
        <span>[Admin]</span>
        <%= "Outils".in_a(href: 'unan_admin/dashboard') %>
        <span> - </span>
        <%= "Calendrier général".in_a(href:"abs_pday/cal?in=unan/program") %>
      </div>
    <% end %>

<% end %>

<%
ajout_abonned =
  if true #user.subscribed?
    "<strike>#{Unan.tarif_humain}</strike> (<b>#{(Unan.tarif - site.tarif)}0 € seulement</b> puisque vous êtes déjà abonné#{user.f_e} !)"
  else
    "#{Unan.tarif_humain} pour toute une année"
  end
lien_subscribe_program = (
  "S’inscrire au programme".in_a(href:"unan/paiement", style:'color:white;font-size:16pt') +
  "<br>#{ajout_abonned}".in_span(class: 'tiny center')
  ).in_div(class: 'btn', style: 'background-color:green;color:white;display:inline-block;text-align:center;padding:1em 2em')


if user.identified?

  # Un user identifié
  # Il peut avoir ou non commencé le programme un an un script, on le
  # sait grâce à unanunscript?

%>
<%

  if user.unanunscript?

      # Un user qui suit le programme
      #
      # NOTE : Pas de titre ici, c'est le bureau qui le contient
%>
  <%= Unan::view("bureau/home") %>
<%

  else

      # Un user identifié mais qui ne suit pas le programme
      # un an un script
%>
  <p class='small right red'>Vous ne suivez pas (encore) le programme “Un An Un Script”.</p>
  <div class="center air"><%= lien_subscribe_program %></div>
  <p class='right air'>
    <%= "#{DOIGT}Découvrir le programme UN AN UN SCRIPT en profondeur".in_a(href:"aide/overview?in=unan") %>
  </p>
<%

  end # if unanunscript?

%>

<%


else

  # Un visiteur qui n'est pas identifié
  # On lui présente simplement le panneau explicatif avec des liens pour
  # s'inscrire au programme ou pour en savoir plus.

%>
<div class="right"><%= lien_subscribe_program %></div>
<p>
  Le pari de ce <strong>programme d'écriture</strong> est simple : que vous soyez auteur expérimenté ou non, que vous ayez ou non des connaissances poussées en dramaturgie, que vous ayez ou non déjà mené à terme un projet d'écriture, ce programme sérieux et inventif s'engage à vous faire <strong>parvenir à écrire votre roman, votre scénario de film ou de BD</strong> en un temps donné d'environ un an — suivant le rythme choisi —, tout en vous faisant suivre une <strong>formation complète à la narration</strong> (l'art d'écrire des histoires).
</p>
<p class='right'>
  <%= "#{DOIGT}En apprendre plus sur ce programme".in_a(href:"aide/overview?in=unan") %>
</p>

<%
end
%>
